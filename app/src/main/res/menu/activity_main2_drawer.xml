<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android">

    <group android:checkableBehavior="single">
        <item
            android:id="@+id/nav_home"
            android:icon="@drawable/ic_menu_home"
            android:title="Home" />
        <item
            android:id="@+id/nav_run"
            android:icon="@drawable/ic_menu_run"
            android:title="Calculate Run" />
        <item
            android:id="@+id/nav_saved_runs"
            android:icon="@drawable/ic_menu_saved_run"
            android:title="Saved Runs" />
        <item
            android:id="@+id/nav_rr"
            android:icon="@drawable/ic_menu_resources"
            android:title="Running Resources" />
    </group>

    <item android:title="Communicate">
        <menu>
            <item
                android:id="@+id/nav_share"
                android:icon="@drawable/ic_menu_share"
                android:title="Share" />
            <item
                android:id="@+id/nav_send"
                android:icon="@drawable/ic_menu_send"
                android:title="Email Suggestions" />
            <item
                android:id="@+id/made_by"
                android:title="Design: © RABeech" />
        </menu>
    </item>

</menu>
